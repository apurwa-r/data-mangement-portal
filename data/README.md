# Sample Data

This directory contains sample documents for local testing.

## Usage

Place test files here (PDF, DOCX, TXT, MD, XLSX) to test the ingestion pipeline locally.

## Notes

- Files in this directory are for development/testing only
- Not committed to git (excluded via `.gitignore` if needed)
- Use small files for quick iteration

